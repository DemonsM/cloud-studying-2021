server:
  port: 8761

spring:
  application:
    name: EUREKA-SERVER

eureka:
  client:
    service-url:
      # 指定注册中心的地址 暴露服务地址
      defaultZone: http://localhost:8761/eureka
    # 关闭eureka的立即注册
    fetch-registry: false
    # 关闭自我注册
    register-with-eureka: false



